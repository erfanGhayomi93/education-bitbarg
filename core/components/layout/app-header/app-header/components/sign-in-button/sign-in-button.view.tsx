import { Button } from "@mui/material";

type PropTypes = {};
export default function SignInButtonView(props: PropTypes) {
  return (
    <Button variant="contained" color="success">
      ورود یا ثبت‌نام
    </Button>
  );
}
